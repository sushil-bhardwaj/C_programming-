#include <stdio.h>
int main(){
    int a=46;
    int b =7;

    int remainder =a % b; // basically this sign(%) is used to take theremainder;
    printf("when a is divided by b the remainder is %d",remainder);
    return 0;
}